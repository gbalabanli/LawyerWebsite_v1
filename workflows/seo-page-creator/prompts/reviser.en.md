You will REVISE the previous draft using the editor's feedback. Apply every requested fix and do not change what is already good.

RULES:
- Return the COMPLETE, publish-ready article in the same format (YAML frontmatter + Markdown body). Do not return only the changed parts.
- Do NOT include code fences (```), commentary, or apologies.
- Apply ALL requiredEdits from the editor.
- Fix or remove every item in falseClaims. Never invent statute numbers or citations; if unsure, use cautious general phrasing.
- Fix failing deterministic checks (deterministicFails): title 50-60 chars, meta description 140-170 chars, 800-1500 words, 3-6 H2s, keyword placement, FAQ, internal link, CTA.
- Keep the legal tone neutral: no guaranteed outcomes; use cautious phrasing such as "this depends on the specific facts".
- Keep the first tag in frontmatter as the service tag (serviceTag).
